export const gameSettings = {
    wordsList: null,
    wordLength : null,
    secretWord : null,
    secretWordObj : {}
}

export const gameState = {
    currentRow : 0,
    currentTile : 0,
    guesses : []
}